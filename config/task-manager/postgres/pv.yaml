apiVersion: v1
kind: PersistentVolume
metadata:
  name: task-manager-postgres-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /kubernetes/task-manager/postgres